apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsany-saas-k8s.fullname" . }}
  labels:
    {{- include "opsany-saas-k8s.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    {{- include "opsany-saas-k8s.selectorLabels" . | nindent 4 }}
