apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsany-saas-apm.fullname" . }}
  labels:
    {{- include "opsany-saas-apm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    {{- include "opsany-saas-apm.selectorLabels" . | nindent 4 }}
